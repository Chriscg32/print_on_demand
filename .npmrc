# Enable legacy peer deps for compatibility
legacy-peer-deps=true

# Don't enforce engine requirements
engine-strict=false

# Save exact versions for more reproducible builds
save-exact=true
save-prefix=""

# Set registry
registry="https://registry.npmjs.org/"

